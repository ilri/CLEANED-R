# this code installes the R packages that are required to run CLEANED

# this code needs to be run after the first installation of R/R studio, and before running CLEANED
# this only needs to be done once 

#you need to be connected to internet to do this. 

install.packages("sp")
install.packages("raster")
install.packages("maptools")
install.packages("RColorBrewer")
install.packages("rgdal")
install.packages("igraph")
install.packages('gridExtra')
install.packages('shiny')
install.packages('shinydashboard')
install.packages("rgeos")
